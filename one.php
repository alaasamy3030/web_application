<?php 
  
  
  if($_GET["pdf"]==1){    
// The location of the PDF file 
// on the server 
$filename = "pdf/pdf1.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
   if($_GET["pdf"]==11){
	  
	  
// The location of the PDF file 
// on the server 
$filename = "pdf/pdf11.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
  
  
  
//////////////////////////////////////
  if($_GET["pdf"]==2){    
// The location of the PDF file 
// on the server 
$filename = "pdf/pdf2.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
   if($_GET["pdf"]==22){
	  
	  
// The location of the PDF file 
// on the server 
$filename = "pdf/pdf22.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
////////////////////////////////////////////////  
 if($_GET["pdf"]==3){    
// The location of the PDF file 
// on the server 
$filename = "pdf/pdf3.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
   if($_GET["pdf"]==33){
	  
	  
// The location of the PDF file 
// on the server 
$filename = "pdf/pdf33.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
  
  
  
//////////////////////////////////////
  if($_GET["pdf"]==4){    
// The location of the PDF file 
// on the server 
$filename = "pdf/pdf4.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
   if($_GET["pdf"]==44){
	  
	  
// The location of the PDF file 
// on the server 
$filename = "pdf/pdf44.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
////////////////////////////////////////////////  
  
  
  
  
  
  
  
  
  
  
  
  
  
    if($_GET["pdf"]==5){    
// The location of the PDF file 
// on the server 
$filename = "pdf/pdf5.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
   if($_GET["pdf"]==55){
	  
	  
// The location of the PDF file 
// on the server 
$filename = "pdf/pdf55.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
  
  
  
//////////////////////////////////////
  if($_GET["pdf"]==6){    
// The location of the PDF file 
// on the server 
$filename = "pdf/pdf6.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
   
////////////////////////////////////////////////  
 if($_GET["pdf"]==7){    
// The location of the PDF file 
// on the server 
$filename = "pdf/pdf7.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
   if($_GET["pdf"]==77){
	  
	  
// The location of the PDF file 
// on the server 
$filename = "pdf/pdf77.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
  
  
  
//////////////////////////////////////
 
////////////////////////////////////////////////  
  
    if($_GET["pdf"]==100){
	  
	  
// The location of the PDF file 
// on the server 
$filename = "pdf/100.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
  
  if($_GET["pdf"]==110){
	  
	  
// The location of the PDF file 
// on the server 
$filename = "pdf/110.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  } 
 if($_GET["pdf"]==120){
	  
	  
// The location of the PDF file 
// on the server 
$filename = "pdf/120.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }  
  
  
   if($_GET["pdf"]==130){
	  
	  
// The location of the PDF file 
// on the server 
$filename = "pdf/130.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
  
  
  
  
  
  
   if($_GET["pdf"]==140){
	  
	  
// The location of the PDF file 
// on the server 
$filename = "pdf/140.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
  
  
  
   if($_GET["pdf"]==150){
	  
	  
// The location of the PDF file 
// on the server 
$filename = "pdf/150.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
  
  
  
  
  
  
  
   if($_GET["pdf"]==160){
	  
	  
// The location of the PDF file 
// on the server 
$filename = "pdf/160.pdf"; 
  
// Header content type 
header("Content-type: application/pdf"); 
  
header("Content-Length: " . filesize($filename)); 
  
// Send the file to the browser. 
readfile($filename); 
	  
  }
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
?> 